package data;

/**
 * An enum holding the different types of apparel.
 * @author William King
 */
public enum ApparelType {
	/** A pair of flippers, used to swim across water tiles. */
	FLIPPERS,
	
	/** A pair of fire boots, used to cross fire tiles. */
	FIREBOOTS
}